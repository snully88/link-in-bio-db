<h1> Hello, world! </h1>

<%= Item.all.each do |the_item| %>
  <div>
    <%= the_item.link_url %>
      <a href="<%= the_item.link_url %>">
        <%= the_item.link_description %>
      </a>
  </div>
<% end %>
